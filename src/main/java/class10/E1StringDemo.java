package class10;

public class E1StringDemo {
    public static void main(String[] args) {
        //we have created an object from the String class and have stored
        //Cat inside it.
        String obj=new String("Cat");
        //Shortcut to create objs from String class
        String obj2="My Dog";
        int len=obj2.length();  //calling the length method/function/action/behaviour
        System.out.println(len);

    }
}
